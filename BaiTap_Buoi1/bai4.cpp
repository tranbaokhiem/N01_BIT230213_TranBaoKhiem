#include <stdio.h>

int main() {
    // Biến hợp lệ
    int age;
    int _id;
    int user_name;

    // Biến không hợp lệ
    // int 123number;   Lỗi: Tên biến không được bắt đầu bằng số.
    // int my-age;      Lỗi: Không được chứa ký tự đặc biệt trừ dấu gạch dưới.
    // int float;       Lỗi: Không được trùng với từ khóa của ngôn ngữ.

    return 0;
}